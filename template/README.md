# __NAME__

What this repo is for, in a paragraph a stakeholder can read.

Built on [monomono](https://github.com/content-jet/monomono): `just` is the door, buck2 is the build graph, `AGENTS.md` is the contract.

```
just setup
just check
```
