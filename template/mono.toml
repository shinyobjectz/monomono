# monomono manifest. `just mono update` rewrites [monomono]; the rest is yours.

[monomono]
version = "0.0.0"
mode = "submodule"
path = "packages/monomono"
repo = "https://github.com/shinyobjectz/monomono"

[repo]
name = "__NAME__"

[modules]
context = "true"
