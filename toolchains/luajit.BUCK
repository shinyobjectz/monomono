# luajit: hermetic LuaJIT 2.1 (pinned commit) as toolchains//:lua; bytecode via luajit -b
load("@monomono//rules/lua:toolchain.bzl", "luajit_hermetic_toolchain")
luajit_hermetic_toolchain(
    name = "lua",
)
